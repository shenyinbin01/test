# Next Step Recommendation

建议下一步先做人工审查，而不是直接进入 skill-pack patch。

## 推荐顺序

1. 项目负责人审查本设计包。
2. 选择一个已验证 Human Texture 片段作为 one-scene MVP。
3. 只生成 brief 层 A/B/C，不生成正文。
4. 用 reviewer gate 判断 C 是否比 B 多出行动主权。
5. 如果通过，再设计实验分支 prompt patch。

## 需要拍板的问题

- Character Agency MVP 第一场景选 C4 关系节点，还是 C3 信息露出节点。
- consequence ledger 是否先按章节维护，还是先按场景维护。
- relationship debt ledger 是否进入 Planner 输出，还是先做 Reviewer 记录。
- 是否允许 C 组 brief 比 B 组略长。

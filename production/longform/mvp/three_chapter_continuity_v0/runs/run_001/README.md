# Run 001 Scaffold

This directory is the input scaffold for a future Longform three-chapter MVP run.

Current status:

- No fiction is generated.
- Hermes is not started.
- DeepSeek is not called.
- No real experiment is run.
- This scaffold only records the approved abstract scenario, output path, reviewer gate, state delta review mode, manual threshold, and failure stop rules.

## Approved Abstract Scenario

Scenario: small organization advancement / assessment.

This is a structural test topic only. It is not a real longform outline, not a complete IP universe, and not a publishable story.

## Directory Map

- `input/`: abstract run inputs and fixed boundaries.
- `orchestrator/`: expected Orchestrator output shapes and placeholders.
- `generation/`: generation output location, currently blocked.
- `review/`: reviewer gate and expected review outputs.
- `state/`: state delta template and reducer rules.
- `reports/`: expected final report shapes.

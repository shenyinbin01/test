# Hermes Execution Packet

This packet gives Hermes everything needed to execute Run003 formally.

Hermes must generate three chapters for each C variant using the same input package. Hermes must not generate from memory or alter shared inputs between variants.

Primary rule: generate prose, but keep engine artifacts private and return structured results for review.

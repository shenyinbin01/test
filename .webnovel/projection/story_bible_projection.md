# .webnovel/projection/story_bible_projection.md
# Story Bible 投影 — 从 .story-system/MASTER_SETTING.yaml 生成
#
# 由 webnovel-state-manager Skill 维护，在 runtime_canon 更新后同步刷新。
# 为 Python 工具层（如 build_wps_projection.py）提供扁平化的 Story Bible 快照。
#
# 格式：Markdown 段落式，不使用 YAML 引用，确保 Python 无需 YAML 即可读取。
